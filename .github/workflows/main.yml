name: Trigger Semgrep Scan
on:
  workflow_dispatch:  # This will allow you to manually trigger the workflow

jobs:
  sast:
    uses: clubpay/secureflow/.github/workflows/sast.yml@Upload-SAST-to-DD
    secrets: inherit
      
    
